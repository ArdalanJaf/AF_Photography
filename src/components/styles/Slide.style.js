import styled from "styled-components";

// a slide is one section of one page site.
const StyledSlide = styled.div`
  min-height: 100vh;
  min-width: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 5%;
`;

export default StyledSlide;
